#!/usr/bin/env sh

# Only crates Homebrew has no formula for belong here. Anything bottled goes
# in the Brewfile so a fresh bootstrap doesn't compile it from source.

cargo_install() {
  cargo install "$1"
}

cargo_install 'cargo-whatfeatures'

cargo_install 'rusty-tags'
cargo_install 'proximity-sort'
